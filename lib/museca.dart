import 'package:flutter/material.dart';

class MusecaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Museca 1+1/2'),
      ),
      body: Center(
        child: Text('Test Content for Museca settings and patches'),
      ),
    );
  }
}
