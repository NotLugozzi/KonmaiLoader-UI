import 'package:flutter/material.dart';

class VividPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sound Voltex:VividWave'),
      ),
      body: Center(
        child: Text('Test Content for the SDVX:VW settings'),
      ),
    );
  }
}
